sudo: false

services:
  - docker

before_install:
- docker build -t turicreate/build-python:2.7 -f docker/Dockerfile-python2.7-build -v $(pwd):/turicreate .

script:
- docker run turicreate/build-python:2.7 "/bin/sh -c /turicreate/build_python_wheel.sh"